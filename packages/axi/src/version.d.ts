// Injected by scripts/build.ts's esbuild `define` from package.json's
// version — this file ships bundled with no adjacent package.json to read
// at runtime, unlike a normal npm-installed CLI.
declare const __BESSEL_AXI_VERSION__: string;
